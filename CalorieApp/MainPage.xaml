<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CalorieApp.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label
                Text="Calorie Tracker"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Button
                x:Name="AddDishBtn"
                Text="Add Dish"
                SemanticProperties.Hint="Adds a dish to the database"
                Clicked="OnAddDishClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="RemoveDishBtn"
                Text="Remove Dish"
                SemanticProperties.Hint="Removes a dish from the database"
                Clicked="OnRemoveDishClicked"
                HorizontalOptions="Fill" />
            />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>